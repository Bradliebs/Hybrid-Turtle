# Environment variables

# Database (SQLite — no external database needed)
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-change-in-production"
CRON_SECRET="set-a-strong-random-secret"

# Market Data — Yahoo Finance (no API key needed!)
# Legacy Alpha Vantage key (optional, not used)
# ALPHA_VANTAGE_API_KEY="your-alpha-vantage-key"

# Market Data Provider — switch between yahoo (default) and eodhd
# Set to "eodhd" to use EODHD Financial APIs instead of Yahoo Finance
MARKET_DATA_PROVIDER="yahoo"
# EODHD API key (required only when MARKET_DATA_PROVIDER=eodhd)
# Get your key at https://eodhd.com/
# EODHD_API_KEY="your-eodhd-api-key"

# Optional data source fallbacks (for the resilient fallback chain)
# Add a key to activate — no code changes needed, provider activates automatically
# ALPHA_VANTAGE_API_KEY=
# EODHD_API_KEY=

# Telegram Bot (optional — for trade alerts)
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
TELEGRAM_CHAT_ID="your-chat-id"

# Nightly Run Schedule (cron expression - 9:30 PM UK)
NIGHTLY_CRON="30 21 * * *"
