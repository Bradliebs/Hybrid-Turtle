# Hybrid Turtle
Trading System using a Hybrid Turtle approach

## Windows Quick Start
- Run `install.bat` once to set up dependencies.
- Use `start.bat` to launch the dashboard.
- `run-dashboard.bat` is a compatibility alias that redirects to `start.bat`.
- Full end-user documentation: `USER-GUIDE.md`
